import styled from "styled-components";
import assetsPath from "../../data/assets-path";

const QuitGame = styled.object`
  position: fixed;
  top: 1em;
  right: 2em;
`

export default <QuitGame data={assetsPath.cross}/>